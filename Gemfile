source "https://rubygems.org"
git_source(:github) { |repo_name| "https://github.com/#{repo_name}" }

ruby "3.0.0"

group :test do
  gem "rspec"
  gem "rubocop"
  gem "rubocop-rspec"
  gem "simplecov"
end
